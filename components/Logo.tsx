import React from "react";

type Props = {};

const Logo = (props: Props) => {
  return (
    <span className="">
      MY<span className="font-black">AI</span>COACH*
    </span>
  );
};

export default Logo;
